import App from "../App";

/**
 * Top-level routing component.
 * Replace this with `react-router` or your preferred router
 * and delegate to feature-level routes as the app grows.
 */
export function AppRoutes() {
  return <App />;
}
